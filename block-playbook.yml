- name: Install and start Apache
  block:
    - name: Install Apache
      apt:
        name: apache2
        state: present

    - name: Start Apache
      service:
        name: apache2
        state: started
  when: ansible_facts['os_family'] == "Debian"
